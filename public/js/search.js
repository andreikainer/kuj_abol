(function()
{
 /* Setting the height of error message for search result as 40% of a current window height */
    var height = $( window ).height()*40/100;
    $('.dependsOnWindowH').css('height', height);

})();